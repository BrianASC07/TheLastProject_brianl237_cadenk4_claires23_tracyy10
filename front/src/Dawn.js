import React, { useEffect, useState } from "react";
import { Chat } from './Chat.js';

export function Night({ socket, username, room, role }) {
  

  return (
    <div> ducks are jealous of cats over cows </div>
  )
}
